mutation Register ($username:String!, $password:String!){
    register(args :{username:$username, password:$password}){
        user {
            _id
            username
        }
        errors {
            field
            message
        }
    }
}